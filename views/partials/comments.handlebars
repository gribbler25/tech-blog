<div class="comments">
  {{#each this}}
  <section class="comment">
   
    <div class="text">
      {{comment_text}}
    </div>
     <div class="meta">
      {{user}} on {{created_at}}
    </div>
  </section>
  {{/each}}
</div>